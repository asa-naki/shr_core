[defaults]
# ロールパスの設定（ansible/rolesディレクトリを含める）
roles_path = ../roles:./roles:~/.ansible/roles:/usr/share/ansible/roles:/etc/ansible/roles

# インベントリファイルのデフォルト設定
inventory = inventory.ini

# その他の基本設定
host_key_checking = False
retry_files_enabled = False
gathering = smart
fact_caching = memory
